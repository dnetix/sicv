; Tuned for Docker on Windows: every file stat crosses the slow bind-mount
; bridge, so keep resolved paths cached long. Opcache settings come from the
; PHP_OPCACHE_* environment variables in docker-compose.yml.
realpath_cache_size=16M
realpath_cache_ttl=3600
